---
layout: layouts/base.njk
title: Unlimited Wash Club
description: Uptown Car Wash Unlimited Wash Club - Wash as often as you like from $29.99/month. Half off your first month. No long-term contract.
permalink: /unlimited-wash/
---

<section class="page-hero">
  <div class="container">
    <h1>{{ memberships.intro.title }}</h1>
    <p>{{ memberships.intro.subtitle }}</p>
  </div>
</section>

<section class="benefits-section">
  <div class="container">
    <div class="benefits-grid">
      {% for benefit in memberships.intro.benefits %}
      <div class="benefit-item">
        <div class="benefit-icon">{{ benefit.icon | safe }}</div>
        <h3>{{ benefit.title }}</h3>
        <p>{{ benefit.desc }}</p>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<section class="services-pricing">
  <div class="container">
    <h2>Choose Your Plan</h2>
    <div class="pricing-grid membership-grid">
      {% for plan in memberships.plans %}
      <div class="pricing-card{% if plan.featured %} featured{% endif %}">
        {% if plan.badge %}<div class="pricing-badge">{{ plan.badge }}</div>{% endif %}
        <h3>{{ plan.name }}</h3>
        <div class="price">{{ plan.price }}<span>/mo</span></div>
        <div class="first-month">First month: {{ plan.first_month }}</div>
        <ul class="pricing-features">
          {% for feature in plan.features %}
          <li>{{ feature }}</li>
          {% endfor %}
        </ul>
        {% if plan.featured %}
        <a href="#signup-plan1" class="btn btn-primary">SIGN UP NOW</a>
        {% elif plan.name == "Plan #2" %}
        <a href="#signup-plan2" class="btn btn-primary">SIGN UP NOW</a>
        {% elif plan.name == "Plan #3" %}
        <a href="#signup-plan3" class="btn btn-primary">SIGN UP NOW</a>
        {% elif plan.name == "Plan #4" %}
        <a href="#signup-plan4" class="btn btn-primary">SIGN UP NOW</a>
        {% else %}
        <span class="coming-soon-btn">Online Payment Coming Soon</span>
        <p class="signup-note">Sign up in person at our location</p>
        {% endif %}
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<section class="signup-section" id="signup-plan1">
  <div class="container">
    <h2>Sign Up for Plan #1 VIP Membership</h2>
    <p>Complete your membership signup below</p>
    <div class="signup-iframe-wrapper">
      <iframe
        src="https://app.flexwash.com/sign-up-portal?organizationId=79&carWashId=52&packageId=432&promotionId=259"
        style="width: 100%; min-height: 600px; border: none;"
        frameborder="0"
        title="VIP Membership Signup">
      </iframe>
    </div>
  </div>
</section>

<section class="signup-section" id="signup-plan2">
  <div class="container">
    <h2>Sign Up for Plan #2 Membership</h2>
    <p>Complete your membership signup below</p>
    <div class="signup-iframe-wrapper">
      <iframe
        src="https://app.flexwash.com/sign-up-portal?organizationId=79&carWashId=52&packageId=431&promotionId=260"
        style="width: 100%; min-height: 600px; border: none;"
        frameborder="0"
        title="Plan #2 Membership Signup">
      </iframe>
    </div>
  </div>
</section>

<section class="signup-section" id="signup-plan3">
  <div class="container">
    <h2>Sign Up for Plan #3 Membership</h2>
    <p>Complete your membership signup below</p>
    <div class="signup-iframe-wrapper">
      <iframe
        src="https://app.flexwash.com/sign-up-portal?organizationId=79&carWashId=52&packageId=430&promotionId=261"
        style="width: 100%; min-height: 600px; border: none;"
        frameborder="0"
        title="Plan #3 Membership Signup">
      </iframe>
    </div>
  </div>
</section>

<section class="signup-section" id="signup-plan4">
  <div class="container">
    <h2>Sign Up for Plan #4 Membership</h2>
    <p>Complete your membership signup below</p>
    <div class="signup-iframe-wrapper">
      <iframe
        src="https://app.flexwash.com/sign-up-portal?organizationId=79&carWashId=52&packageId=429&promotionId=262"
        style="width: 100%; min-height: 600px; border: none;"
        frameborder="0"
        title="Plan #4 Membership Signup">
      </iframe>
    </div>
  </div>
</section>

<section class="how-it-works">
  <div class="container">
    <h2>How It Works</h2>
    <div class="steps-grid">
      {% for step in memberships.how_it_works %}
      <div class="step">
        <div class="step-number">{{ step.step }}</div>
        <h3>{{ step.title }}</h3>
        <p>{{ step.desc }}</p>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<section class="cta-area">
  <div class="container">
    <h2>Ready to Join?</h2>
    <p>Stop by today to sign up for your Unlimited Wash Club membership</p>
    <div class="cta-buttons">
      <a href="{{ settings.maps_url }}" class="btn btn-primary" target="_blank">GET DIRECTIONS</a>
      <a href="tel:{{ settings.phone_link }}" class="btn btn-secondary">CALL {{ settings.phone }}</a>
    </div>
  </div>
</section>
